import { createAction } from 'redux-actions'

export const setEpicPhase = createAction('SET_EPIC_PHASE')
export const createPhase = createAction('CREATE_PHASE')
export const deletePhase = createAction('DELETE_PHASE')
